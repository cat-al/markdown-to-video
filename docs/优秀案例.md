镜头 1（0 - 1.2 秒）
话术：“Harness Engineering 是什么”
元素：卡通银发女孩骑灰色机械马，背景是夕阳下的战场（有断矛、烟雾），左上角出现 “Harness Engineering” 文字。
镜头 2（1.2 - 3.7 秒）
话术：“和提示词工程”“有什么关系”
元素：画面中马继续奔跑，右上角出现 “Prompt Engineering” 文字；随后场景变暗，天空乌云密布，右上角新增 “Context Engineering” 文字，战场元素更显阴沉。
镜头 3（3.7 - 6.2 秒）
话术：“2026 年”“OpenAI 在一篇博客文章提到了 Harness Engineering”
元素：地球动画（标注 “2026 年”），切换到 OpenAI 博客截图，标题为 “Harness engineering: leveraging Codex in an agent-first world”。
镜头 4（6.2 - 11.6 秒）
话术：“驾驭工程”“之后就快速在 AI 圈里火了起来”
元素：博客标题下方出现 “驾驭工程” 中文翻译；画面切换为 “Harness Engineering 驾驭工程” 文字，下方配有三个火焰图标。
镜头 5（11.6 - 18 秒）
话术：“很多人根本不知道它到底是什么就开始各种跟风吹爆”“这在三天一‘重磅’，五天一‘炸裂’的 AI 圈里”
元素：浏览器界面，显示 “Harness Engineering” 的多种中文翻译（牛码工程、驾驭工程、马具工程、驱码工程），卡通角色旁边环绕 “震惊！重磅！天塌了！好炸裂！牛爆了” 等夸张红色文字。
镜头 6（18 - 24 秒）
话术：“那它到底是什么”“和提示词工程、上下文工程又是什么关系”
元素：卡通银发女孩叉腰，右侧电脑屏幕显示 “Harness Engineering 驾驭工程”，左侧依次出现 “Prompt Engineering 提示词工程”“Context Engineering 上下文工程” 文字。
镜头 7（24 - 29 秒）
话术：“全网资料参差不齐 以我为准”“今天就把这些概念串起来讲透”
元素：卡通女孩在堆满书籍、文件的杂乱房间，手持写有 “Harness Engineering” 的蓝色书籍；画面切换为 Harness、Prompt、AI Agent、Context、LLM 的图标阵列。
镜头 8（29 - 32 秒）
话术：“看完你就会知道 AI Agent 的开发本质上是在做什么”
元素：各图标间出现箭头连接，形成概念关联的流程图。
镜头 9（32 - 40 秒）
话术：“AI Agent 的开发本质上是在做什么”“为什么同样的模型换个 AI IDE 效果会差这么多”“程序员就不写代码 是真的吗”
元素：AI Agent 图标放大；对比 LLM 图标；出现卡通肌肉程序员（穿 “Agent Skills” 红 T 恤），周围环绕 “AI 编程 全是 AI 代码”“拥抱 AI 看不懂 能运行” 等夸张文字。
镜头 10（40 - 43 秒）
话术：“看之前你 关注了吗”
元素：画面切换为绿色点赞图标和 “FOLLOW” 关注按钮。
以下是镜头 11（43 秒）开始的详细镜头拆解：
镜头 11（43 - 45 秒）
话术：“Prompt Engineering”
元素：画面展示蓝色的 “Prompt Engineering” 文字，背景为浅灰色，无其他元素，突出主题。
镜头 12（45 - 56 秒）
话术：“把 ChatGPT、Claude 的外壳剥开，里面的大模型也就是 LLM，本质就是一个磁盘上的超大参数文件。将它加载到显卡内存里配上 HTTP 接口就成了大模型 API 服务，加个聊天界面就变成了聊天 AI，加个代码编辑器就成了 AI IDE”
元素：
先出现 ChatGPT 和 Claude 的图标，随后 “外壳” 被剥开，露出内部的 “大模型（LLM）” 图示。
展示磁盘上的 “gpt-5.bin”“claude-4.6.bin” 超大参数文件图标。
这些文件被加载到显卡，形成 “推理服务” 图标；推理服务配上 “HTTP 接口” 后，组成 “大模型 API 服务”（包含 “网关”“计费”“调度” 模块）。
大模型 API 服务加 “聊天界面” 图标，变成 “聊天 AI”；加 “代码编辑器” 图标，变成 “AI IDE”。
镜头 13（56 - 70 秒）
话术：“大模型做的事情很简单，就是基于当前输入的内容预测下一个字词，大概率会是什么，本质上只是在猜你想要什么”
元素：
展示 “推理服务” 图标，输入文字 “问君能有几多愁”，箭头指向推理服务后，输出 “恰似_”。
输出旁出现 “骰子” 图标，暗示大模型 “猜” 的本质。
镜头 14（70 - 87 秒）
话术：“输入的指令太宽泛，那它预测的答案就会非常发散。比如你丢给它一段代码说‘加个排序’，它可能只回你排序的那部分，你得补一句‘给我完整的函数代码，不要乱改我的代码’，它给的结果才会更符合要求”
元素：
输入 “写个代码让我看看”，箭头指向推理服务后，输出 “Python？Java？Golang？”，体现回答发散。
展示代码示例：def process(nums): total = sum(nums) print(total)，输入 “加个排序”，输出 “在 sum 之前加 sort (nums) 就好啦”。
补全指令 “给我完整的代码！不要乱改我的代码！” 后，输出完整代码def process(nums): slist = sorted(nums) print(total) total = sum(nums) print(total)。
镜头 15（87 - 96 秒）
话术：“能加的内容有很多，比如角色设定、背景、历史对话、参考文档、限制、输出格式，这些约束构成了所谓的提示词。而这种有意识的调整和设计提示词，让模型稳定的朝着你预期的内容和格式输出的技术手段就是所谓的提示词工程，它解决的是大模型无引导乱说话的问题”
元素：
展示提示词示例，包含 “角色设定：你是资深 Python 工程师”“背景：我现在需要优化一段数据处理代码”“历史对话：我前面提到 xxx”“参考文档：参照 Python3 官方文档”“限制：不改原有代码逻辑，只新增排序功能”“输出格式：直接返回完整可运行的函数代码，不要多余文字解释” 等模块，这些模块被框选标注为 “提示词”。
提示词箭头指向 “推理服务”，出现 “实际” 与 “预期” 的偏差图示，表明提示词工程让输出更贴合预期。
最后框选 “提示词工程”，说明其解决大模型无引导乱说话的问题。
镜头 16（96 - 110 秒）
话术：“Context Engineering”
元素：画面展示蓝色的 “Context Engineering” 文字，背景为浅灰色，无其他元素，突出主题。
镜头 17（110 - 127 秒）
话术：“提示词写得越长越仔细，模型知道的就越多，回答就越准。反过来同理，大模型回答不准，那大概率是因为知道的不够多。于是大家很自然会不断往大模型里塞各种资料，这些打包到一起发给大模型的所有信息就叫上下文。提示词只是上下文的一部分”
元素：
展示提示词示例（延续之前的详细提示词框），提示词内容逐渐变长，旁边出现 “灯泡” 图标表示模型理解增加；反之提示词变短，“灯泡” 图标消失，出现 “阿巴阿巴” 的无效回答。
动画演示 “提示词 + 其他资料” 打包成一个整体，标注为 “上下文”，并说明 “提示词是上下文的一部分”。
镜头 18（127 - 149 秒）
话术：“但大模型再强，一次性能处理的上下文也有最大限制，这个限制叫上下文窗口。在 AI 大模型应用里，多对话几轮就很容易将上下文窗口打满，于是就需要通过一些策略去压缩或丢弃部分信息。在这个过程中不可避免会丢失关键信息，从而破坏上下文的完整性和准确性，这类问题被统称为上下文腐化。效果上就是模型开始记不住，回答前后不一致”
元素：
展示 “上下文” 文件图标和 “大模型” 的 “上下文窗口” 图标，窗口有容量限制（用黄色箱子示意）。
手机对话界面示例，多轮消息（消息 1、消息 2、消息 3、消息 4）填满窗口后，红色叉号标注丢弃 “消息 1”，导致后续模型回答 “忘了哈哈”“前后不一致”。
镜头 19（149 - 165 秒）
话术：“上下文窗口就这么大，于是问题就变成了怎么才能在合适的时候将合适的内容塞入到有限的上下文当中，于是衍生了一套负责动态管理大模型上下文的技术，也就是所谓的上下文工程。提示词是上下文的一部分，那自然提示词工程其实也是上下文工程的一部分，它一般通过外部程序来实现，比如 Cursor、Claude Code 这类 coding Agent。注意，这不是广告，每一家的技术实现都有差异，但总的来说可以总结为三个步骤：召回、压缩和组装”
元素：
动画演示 “上下文” 如何动态塞入 “上下文窗口”，引出 “上下文工程”。
图示 “提示词工程” 属于 “上下文工程” 的一部分。
出现 Cursor、Claude Code、Trae 等工具图标，标注为 “外部程序”。
展示上下文工程的三个步骤：“召回”“压缩”“组装”，用箭头连接成流程。
镜头 20（165 - 179 秒）
话术：“第一步：召回，说白了就是找什么信息，这些信息可以来自外部新闻，也可以来自过去聊天记录、当前代码环境以及程序运行报错等。总之就是从里面找出最相关的内容，这里面涉及到一些 RAG、memory 等技术，随便拿出一个都能单开一个视频，这里先跳过。看到这里还没睡着的弹幕扣个 0”
元素：
展示 “召回” 步骤，信息来源图标包括 “新闻”“聊天记录”“代码环境”“报错信息” 等。
标注 “RAG”“Memory” 技术，提示这些技术可单独讲解。
出现 “弹幕扣 0” 的互动提示。
镜头 21（179 - 208 秒）
话术：“信息很多，上下文窗口有限，所以需要将信息变小，于是引入第二步：压缩。比如将信息分开发给大模型做总结，之后就是组装。因为信息放置的位置和顺序会直接影响模型的理解和输出，比如越靠后越容易被模型关注，所以需要通过一定的结构重新组装内容，这样进入模型的上下文更精简、更相关，输出也会更稳定、更准确”
元素：
展示 “压缩” 步骤，将多条信息（信息 1、信息 2、信息 3）分开发给大模型做总结。
展示 “组装” 步骤，调整信息顺序（如信息 3、信息 1、信息 2），说明顺序影响模型关注。
最终组装后的上下文箭头指向大模型，输出更稳定准确。
镜头 22（208 - 235 秒）
话术：“不同 AI 工具的上下文工程策略不同，所以你会发现就算用的是同一个模型，不同 AI 工具的执行效果也会有差异。Claude Code 最近也被开源了，正好可以单开一期讲下它的上下文工程是怎么做的。看到这里还在坚持的弹幕扣个 1”
元素：
出现 Cursor、Claude Code、Trae 等工具图标，说明它们策略不同。
展示 Claude Code 界面图示，提示可单开一期讲解。
出现 “弹幕扣 1” 的互动提示。